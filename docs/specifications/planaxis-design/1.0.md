# PlanAxis Design Format Specification

**Version:** 1.0  
**Status:** Normative specification  
**Language:** English  
**Schema identifier:** `planaxis-design/1.0`

---

# 1. Purpose and scope

This document defines **PlanAxis Design Format 1.0**, the persistent renderer-independent format used to describe an interior-design scenario within a PlanAxis project.

A design scenario records design intent associated with exactly one Apartment SVG architectural alternative.

Version 1.0 supports persistent design data for:

- surface-finish assignments;
- presentation overrides for tone mapping and exposure.

The format deliberately separates:

```text
architectural truth
    from
interior-design decisions
    from
renderer implementation state
```

Apartment SVG remains the canonical source of architectural geometry and semantics. A PlanAxis design descriptor does not duplicate, replace, override, or repair architectural geometry.

PlanAxis Design Format 1.0 does **not** define:

- material descriptor semantics;
- texture maps or physical material properties;
- 3D model assets or object placement;
- lighting-design data;
- environment assets or environment-lighting configuration;
- camera navigation or saved-view state;
- lens or aspect-ratio settings;
- post-processing;
- AI-generation instructions;
- scenario inheritance;
- a persistent active-design selection.

Those concerns require separate future specifications or later versions when their semantics are concrete.

---

# 2. Normative keywords

The key words **REQUIRED / MUST**, **MUST NOT**, **SHOULD**, **SHOULD NOT**, **MAY**, and **OPTIONAL** are normative.

- **REQUIRED / MUST** — mandatory for conformance.
- **MUST NOT** — prohibited.
- **SHOULD** — recommended unless a documented reason justifies deviation.
- **SHOULD NOT** — discouraged unless a documented reason justifies deviation.
- **MAY / OPTIONAL** — permitted but not required.

If an example conflicts with a normative rule, the normative rule takes precedence.

---

# 3. Relationship to other PlanAxis formats

PlanAxis Design Format is versioned independently from:

- PlanAxis Project Format;
- Apartment SVG;
- future PlanAxis Material formats;
- future PlanAxis model-asset formats;
- the PlanAxis application version.

A project may therefore combine versions independently, for example:

```text
PlanAxis Project Format 1.0
Apartment SVG 2.2
PlanAxis Design Format 1.0
future PlanAxis Material Format 1.x
```

## 3.1. PlanAxis Project Format

A design descriptor exists inside a filesystem-backed PlanAxis project and uses the project-relative path and filesystem-boundary rules defined by the applicable PlanAxis Project Format specification.

PlanAxis Project Format 1.0 reserves:

```text
designs/
```

for durable design-scenario data and:

```text
assets/materials/
```

for normalized material resources.

This specification gives those design descriptors concrete semantics without changing the root project manifest.

## 3.2. Apartment SVG

Apartment SVG remains the source of architectural and geometric truth.

The design descriptor references one Apartment SVG architecture but MUST NOT duplicate architectural facts such as:

- walls;
- spaces;
- openings;
- dimensions;
- floor or ceiling geometry;
- element positions;
- finish-target geometry.

Finish assignments address architectural surfaces through stable finish-target identifiers derived from the referenced architecture.

## 3.3. PlanAxis Material Format

A `material` reference in Design Format 1.0 identifies a project-local resource location under `assets/materials/`.

Design Format 1.0 deliberately assigns **no internal material semantics** to the referenced resource. Those semantics are independently defined by PlanAxis Material Format 1.0:

```text
docs/specifications/planaxis-material/1.0.md
```

In particular, this version does not define:

- a material schema identifier;
- required descriptor filename;
- required file extension;
- PBR fields;
- texture-map fields;
- physical texture dimensions;
- rendering interpretation.

Material Format 1.0 defines those semantics without changing the basic Design 1.0 finish-assignment relationship.

A Design 1.0 `material` reference remains structurally valid according to this specification even when it does not identify a conforming Material 1.0 descriptor. In particular, Design 1.0 still does not require a `.json` suffix or resource existence. Those requirements are evaluated only when an application resolves the reference as a Material 1.0 material.

---

# 4. Conformance and resolution levels

A design descriptor is examined at distinct levels.

## 4.1. JSON-conformant

The file contains syntactically valid JSON representing exactly one JSON object.

## 4.2. Design-format conformant

The JSON object satisfies all structural and lexical rules of `planaxis-design/1.0`, including:

- supported schema identifier;
- closed object structures;
- required properties;
- project-relative path syntax;
- finish-target identifier syntax;
- finish-assignment uniqueness;
- presentation value constraints.

Design-format conformance does not by itself prove that the referenced architecture exists or that every finish target exists in that architecture.

## 4.3. Project-resolved

A design descriptor is project-resolved when its referenced architecture:

- resolves through the project filesystem boundary;
- is an accessible regular lowercase `.svg` file under `architecture/`;
- is reached without prohibited symbolic-link traversal;
- remains within the canonical project root.

Material-resource existence is explicitly excluded from Design 1.0 project resolution.

## 4.4. Architecture-resolved

A project-resolved design descriptor is architecture-resolved when:

1. the referenced Apartment SVG passes its own complete validation pipeline;
2. its renderer-independent architectural surfaces and finish targets can be derived;
3. every persisted finish target in the design exists in that derived target set.

A descriptor may remain design-format conformant while failing project or architecture resolution.

## 4.5. Material-resolved

Material resolution is **not defined by Design Format 1.0**.

A syntactically valid material reference may identify a resource that does not yet exist or whose contents are not yet interpretable by the current PlanAxis phase. Such a condition does not make the design descriptor structurally non-conformant.

PlanAxis Material Format 1.0 now defines material-descriptor and texture-resource semantics independently. Applying that format remains a separate resolution stage and does not alter Design 1.0 conformance.

---

# 5. Terminology

## 5.1. Design descriptor

A **design descriptor** is a JSON file under `designs/` that conforms to this specification.

## 5.2. Design scenario

A **design scenario** is the persistent design intent represented by one design descriptor.

## 5.3. Design identity

The project-relative path of the design descriptor is its durable identity within the project.

No separate internal design ID exists in Version 1.0.

## 5.4. Design name

The `name` field is a human-readable display label only.

It is not:

- an identifier;
- a filesystem name;
- a uniqueness key;
- a reference target.

## 5.5. Architecture binding

The `architecture` field binds the design scenario to exactly one Apartment SVG document identified by project-relative path.

## 5.6. Finish target

A **finish target** is a stable renderer-independent semantic address for a designable architectural surface or scoped surface coverage derived from the referenced architecture.

## 5.7. Material reference

A **material reference** is a project-relative path under `assets/materials/`.

In Design Format 1.0 it identifies only a resource location. It does not imply any internal material representation.

---

# 6. Design descriptor location, identity, and encoding

## 6.1. Location

A design descriptor MUST be stored at a project-relative path under:

```text
designs/
```

The filename MUST use the lowercase `.json` extension.

Examples:

```text
designs/warm-modern.json
designs/scandinavian.json
designs/concepts/warm-modern.json
designs/alternatives/kitchen-dark.json
```

No additional filename or directory convention is required by Version 1.0.

## 6.2. Identity

The descriptor's complete project-relative path is its identity.

For example:

```text
designs/warm-modern.json
```

and:

```text
designs/archive/warm-modern.json
```

identify different design scenarios even if their contents are otherwise identical.

Moving or renaming a design descriptor therefore changes its identity.

## 6.3. Encoding

The file MUST contain UTF-8 encoded JSON representing exactly one JSON object.

A byte-order mark SHOULD NOT be written.

---

# 7. Project-relative path rules

All persistent paths in a Design 1.0 descriptor MUST use the project-relative path syntax defined by the applicable PlanAxis Project Format specification.

A project-relative path MUST NOT:

- be empty;
- begin with `/`;
- contain `\\`;
- contain an empty path segment;
- contain a `.` path segment;
- contain a `..` path segment;
- contain a NUL character;
- contain a URI scheme such as `file:`;
- contain a Windows drive prefix such as `C:`;
- otherwise represent or resolve as an absolute filesystem path.

Persistent paths use `/` as the separator on every operating system.

---

# 8. Closed-schema rule

PlanAxis Design Format 1.0 is recursively closed.

Unknown properties are PROHIBITED:

- at the root object;
- inside each finish assignment;
- inside the `presentation` object.

Version 1.0 defines no `x-*`, `data-x-*`, or other extension-property mechanism.

A future format version may add properties explicitly.

---

# 9. Root object

The root JSON object supports exactly these properties:

| Property | Type | Required | Meaning |
|---|---|---:|---|
| `schema` | string | yes | schema identifier |
| `name` | string | yes | human-readable display name |
| `architecture` | string | yes | strict project-relative architecture binding |
| `finishes` | array | no | persistent finish assignments |
| `presentation` | object | no | persistent presentation overrides |

A representative document is:

```json
{
  "schema": "planaxis-design/1.0",
  "name": "Warm modern",
  "architecture": "architecture/concept-alt-2/apartment.svg",
  "finishes": [
    {
      "target": "floor",
      "material": "assets/materials/oak-natural/material.json"
    }
  ],
  "presentation": {
    "toneMapping": "agx",
    "exposureEv": 0.7
  }
}
```

The minimum conforming root object contains only the three required properties.

---

# 10. `schema`

`schema` is REQUIRED.

Its value MUST be exactly:

```text
planaxis-design/1.0
```

An implementation that does not support the schema identifier MUST reject Design Format interpretation and MUST NOT guess the semantics of the document.

---

# 11. `name`

`name` is REQUIRED.

Its value MUST be a JSON string containing at least one non-whitespace Unicode character after trimming.

The stored value is the human-readable scenario name.

The `name`:

- MAY be duplicated by another design descriptor;
- MAY differ from the descriptor filename;
- MUST NOT be used as persistent identity.

---

# 12. `architecture`

`architecture` is REQUIRED.

Its value MUST be a project-relative path that:

- uses the path syntax defined in section 7;
- begins with `architecture/`;
- names a file using the lowercase `.svg` extension.

Example:

```json
"architecture": "architecture/concept-alt-2/apartment.svg"
```

The referenced architecture does not need to be the architecture currently selected by `architecture.active` in `planaxis.project.json`. A design may be bound to any project-resolved, valid Apartment SVG architectural alternative under `architecture/`.

The project manifest's active-architecture selection and a design scenario's architecture binding are independent concepts.

## 12.1. Strict architecture binding

The architecture reference has strict semantics:

> The design scenario is defined for exactly the Apartment SVG identified by the `architecture` path.

A design MUST NOT be implicitly applied to another Apartment SVG merely because:

- the other document contains the same element IDs;
- the same finish-target IDs can be derived;
- the geometry appears similar;
- the files are related architectural alternatives.

Matching identifiers are not sufficient evidence of design compatibility.

If a user wants to reuse a scenario with another architecture, that operation must be explicit and must produce or update a descriptor whose `architecture` property identifies the new architecture.

Version 1.0 does not define a dedicated copy, duplicate, or rebind operation.

## 12.2. Architecture existence and validation

Design-format conformance checks only the lexical/path rules above.

For project and architecture resolution, PlanAxis MUST additionally require that the referenced architecture:

1. resolves through the project filesystem boundary;
2. identifies an accessible regular `.svg` file;
3. remains under the canonical project root without prohibited symbolic-link traversal;
4. passes the complete Apartment SVG validation pipeline before finish-target resolution occurs.

If the architecture is missing, inaccessible, invalid, renamed, or otherwise unresolved, the design remains potentially Design 1.0-format conformant but cannot be architecture-resolved or applied as a resolved scenario.

---

# 13. `finishes`

`finishes` is OPTIONAL.

If present:

- it MUST be a JSON array;
- it MUST contain at least one assignment;
- every array item MUST conform to section 13.1;
- no two assignments may use the same `target` value.

An empty array is PROHIBITED.

If there are no finish assignments, the `finishes` property SHOULD be omitted.

## 13.1. Finish assignment object

Each finish assignment is a closed JSON object with exactly these properties:

| Property | Type | Required | Meaning |
|---|---|---:|---|
| `target` | string | yes | finish-target identifier |
| `material` | string | yes | project-relative material resource reference |

Example:

```json
{
  "target": "space:living-room:wall:wall-west:side-positive",
  "material": "assets/materials/paint-warm-white/material.json"
}
```

Unknown properties are PROHIBITED.

## 13.2. Duplicate target assignments

Within one `finishes` array, each `target` value MUST appear at most once.

For example, this is invalid:

```json
"finishes": [
  {
    "target": "floor",
    "material": "assets/materials/oak/material.json"
  },
  {
    "target": "floor",
    "material": "assets/materials/tile/material.json"
  }
]
```

Version 1.0 defines no precedence rule such as "first wins" or "last wins".

---

# 14. Finish-target identifiers

A `target` value MUST conform to one of the lexical forms in this section.

Identifiers embedded inside a finish-target string are Apartment SVG semantic IDs and therefore use the Apartment SVG `Id` lexical form:

```text
[A-Za-z][A-Za-z0-9._-]*
```

Because `:` is not permitted inside an Apartment SVG `Id`, the separators in finish-target identifiers are unambiguous.

## 14.1. Base floor and ceiling

```text
floor
ceiling
```

## 14.2. Wall sides

```text
wall:<wall-id>:side-negative
wall:<wall-id>:side-positive
```

Examples:

```text
wall:wall-living-west:side-negative
wall:wall-living-west:side-positive
```

The side semantics are renderer-independent and are determined by the architectural surface model.

## 14.3. Opening reveals

```text
wall:<wall-id>:opening:<opening-id>:reveal-start
wall:<wall-id>:opening:<opening-id>:reveal-end
wall:<wall-id>:opening:<opening-id>:reveal-top
wall:<wall-id>:opening:<opening-id>:reveal-bottom
```

Examples:

```text
wall:wall-living-west:opening:window-living-01:reveal-start
wall:wall-living-west:opening:door-hall-01:reveal-top
```

Only physically existing reveal targets may resolve successfully.

## 14.4. Space-scoped floor and ceiling targets

```text
space:<space-id>:floor
space:<space-id>:ceiling
```

Examples:

```text
space:zone-living:floor
space:zone-bedroom:ceiling
```

## 14.5. Space-scoped wall-side targets

```text
space:<space-id>:wall:<wall-id>:side-negative
space:<space-id>:wall:<wall-id>:side-positive
```

Example:

```text
space:zone-living:wall:wall-living-west:side-positive
```

## 14.6. Unsupported target forms

Version 1.0 does not define space-scoped opening-reveal targets.

A target such as:

```text
space:zone-living:wall:wall-living-west:opening:window-01:reveal-start
```

is therefore invalid in Design Format 1.0.

## 14.7. Lexical validity versus architectural existence

A syntactically valid `target` does not prove that the target exists in the bound architecture.

For example:

```text
wall:wall-old:side-positive
```

may satisfy the lexical grammar but fail architecture resolution because `wall-old` no longer exists or no corresponding physical target can be derived.

Such a condition is an unresolved/stale design reference, not malformed JSON.

---

# 15. Material references

The `material` property of a finish assignment MUST be a project-relative path that:

- follows section 7;
- begins with `assets/materials/`;
- contains at least one path segment after `assets/materials/`.

Examples:

```text
assets/materials/oak-natural/material.json
assets/materials/paint/warm-white
assets/materials/tiles/bathroom/matte-stone.material
```

Design Format 1.0 intentionally does **not** require:

- a particular filename;
- a particular filename extension;
- the referenced resource to exist;
- the referenced resource to be a regular file;
- the referenced resource to conform to a material schema;
- PlanAxis to interpret or render the referenced resource.

Those checks belong to the independent PlanAxis Material Format 1.0 resolution contract when the resource is intended to resolve as a Material 1.0 descriptor.

A reference outside `assets/materials/` is invalid Design 1.0 data.

For example:

```text
references/materials/oak-photo.jpg
assets/models/chair/model.glb
../shared/material.json
```

are not valid `material` references.

---

# 16. `presentation`

`presentation` is OPTIONAL.

If present:

- it MUST be a JSON object;
- it MUST contain at least one supported property;
- it MUST contain no unknown properties.

An empty object is PROHIBITED.

If no persistent presentation override is required, the `presentation` property SHOULD be omitted.

Supported properties are:

| Property | Type | Required | Meaning |
|---|---|---:|---|
| `toneMapping` | enum string | no | PlanAxis presentation tone-mapping choice |
| `exposureEv` | finite JSON number | no | exposure compensation in EV |

At least one of them MUST be present whenever `presentation` exists.

## 16.1. `toneMapping`

Permitted values are exactly:

```text
agx
aces-filmic
neutral
```

These are PlanAxis-owned renderer-independent vocabulary values.

A rendering adapter maps them to its own backend implementation.

The descriptor MUST NOT persist rendering-library constants, enum numbers, class names, or other implementation-specific values.

If `toneMapping` is omitted, the consuming PlanAxis application uses its current presentation default.

The omission does not imply a persisted schema-level value.

## 16.2. `exposureEv`

`exposureEv` is a finite JSON number representing exposure compensation in photographic EV stops.

Examples:

```json
"exposureEv": 0
```

```json
"exposureEv": -1.5
```

```json
"exposureEv": 6
```

Design Format 1.0 intentionally does not restrict this value to the current browser UI range.

A user interface MAY expose a narrower editing range without changing the persistent format.

If `exposureEv` is omitted, the consuming PlanAxis application uses its current presentation default.

The omission does not imply a persisted schema-level value.

## 16.3. Presentation data not included in Version 1.0

The following are not Design 1.0 presentation properties:

- environment resource;
- environment intensity;
- environment rotation;
- camera selection;
- camera position or orientation;
- orbit/navigation state;
- Walk pose;
- focal length;
- aspect ratio;
- viewport size;
- Focus-view state;
- post-processing settings.

The current runtime environment controls are not persisted because no durable environment-asset contract exists yet.

---

# 17. Numeric semantics

PlanAxis Design Format is not an authoritative geometry format.

JSON numbers in Design 1.0, including `exposureEv`, use ordinary JSON numeric semantics and may be represented as ordinary TypeScript/JavaScript `number` values after validation.

The exact-decimal arithmetic rules used for authoritative Apartment SVG geometry do not apply to Design 1.0 presentation values.

Design descriptors MUST NOT contain redundant architectural geometry merely to avoid resolving the referenced Apartment SVG.

---

# 18. Architecture resolution and stale references

After the bound Apartment SVG has passed its own validation pipeline, PlanAxis derives its renderer-independent architectural surfaces and finish targets.

Every persisted finish assignment MUST resolve against that exact derived target set before the design is considered architecture-resolved.

## 18.1. Stale target

A finish target may become stale when the referenced architecture changes, for example when:

- a wall is removed or renamed;
- an opening is removed or renamed;
- a space is removed or renamed;
- changed geometry causes a previously existing reveal or space-scoped target to no longer exist.

A stale target does not retroactively make the JSON malformed.

PlanAxis SHOULD report unresolved finish targets distinctly from:

- JSON syntax failures;
- Design 1.0 structural failures;
- project-filesystem failures;
- Apartment SVG validation failures.

## 18.2. No implicit repair

PlanAxis MUST NOT silently repair an unresolved target by:

- guessing a replacement wall, space, or opening;
- using a similarly named ID;
- selecting another architecture;
- dropping the assignment without reporting it;
- rewriting the design descriptor during ordinary loading.

Correction requires an explicit design-editing operation.

---

# 19. Application and selection semantics

## 19.1. No persistent active design

PlanAxis Design Format 1.0 defines no project-level or design-level persistent "active design" property.

The PlanAxis Project Format 1.0 manifest remains unchanged and contains no active-design reference.

Which design scenario is currently selected is application/session state.

Selecting a scenario MUST NOT silently modify:

- `planaxis.project.json`;
- the referenced Apartment SVG;
- another design descriptor.

## 19.2. Applying a design

A design may be applied as a resolved scenario only to the exact architecture identified by its `architecture` property.

During the Phase 2 implementation corresponding to Design Format 1.0:

- finish assignments may be loaded, validated, preserved, and reported;
- material references have no required rendering interpretation;
- therefore finish assignments need not change rendered surface appearance until a material format is introduced;
- supported persisted presentation overrides may be applied through the renderer-independent presentation mapping.

---

# 20. Persistence and write behavior

Design descriptors are durable project data.

Creating or modifying a design descriptor is an explicit design-editing operation.

PlanAxis MUST NOT silently rewrite a design descriptor merely because it was opened, selected, validated, or rendered.

When PlanAxis writes or replaces a design descriptor, it SHOULD use an atomic replacement strategy where the host filesystem permits it so interruption does not leave a partially written durable descriptor.

Changing the `architecture` value is an explicit durable design change. It MUST NOT occur automatically because another architecture appears compatible.

---

# 21. Versioning and future evolution

## 21.1. Independent versioning

Design Format versions evolve independently from Project Format, Apartment SVG, material formats, and application versions.

## 21.2. Unsupported versions

An implementation that does not support the descriptor's `schema` value MUST reject Design Format interpretation.

It MUST NOT guess the meaning of an unsupported version.

## 21.3. Future properties

Because Design 1.0 is recursively closed, future capabilities such as:

- 3D object placement;
- lighting design;
- environment assets;
- richer presentation settings;
- saved render views;

require an explicit later version or another accepted versioned contract.

Version 1.0 intentionally does not reserve empty placeholder objects for future concepts.

## 21.4. Scenario inheritance

Version 1.0 defines no design-to-design inheritance, parent scenario, base scenario, or overlay mechanism.

Each descriptor is self-contained with respect to the design choices represented by this version.

---

# 22. Normative examples

## 22.1. Minimal conforming design

```json
{
  "schema": "planaxis-design/1.0",
  "name": "Warm modern",
  "architecture": "architecture/existing.svg"
}
```

This descriptor contains no finish or presentation overrides.

## 22.2. Finish assignments

```json
{
  "schema": "planaxis-design/1.0",
  "name": "Natural oak",
  "architecture": "architecture/existing.svg",
  "finishes": [
    {
      "target": "floor",
      "material": "assets/materials/oak-natural/material.json"
    },
    {
      "target": "space:zone-living:wall:wall-living-west:side-positive",
      "material": "assets/materials/paint-warm-white/material.json"
    },
    {
      "target": "wall:wall-living-west:opening:window-living-01:reveal-top",
      "material": "assets/materials/reveal-white"
    }
  ]
}
```

This document may be Design 1.0-format conformant even if one or more material resources do not exist yet.

All finish targets must nevertheless resolve against `architecture/existing.svg` before the design is architecture-resolved.

## 22.3. Partial presentation override

```json
{
  "schema": "planaxis-design/1.0",
  "name": "Bright presentation",
  "architecture": "architecture/existing.svg",
  "presentation": {
    "exposureEv": 1.2
  }
}
```

The omitted tone mapping uses the consuming application's current presentation default.

## 22.4. Complete Version 1.0 example

```json
{
  "schema": "planaxis-design/1.0",
  "name": "Warm modern",
  "architecture": "architecture/concept-alt-2/apartment.svg",
  "finishes": [
    {
      "target": "floor",
      "material": "assets/materials/oak-natural/material.json"
    },
    {
      "target": "space:zone-living:wall:wall-tv:side-negative",
      "material": "assets/materials/paint-clay/material.json"
    }
  ],
  "presentation": {
    "toneMapping": "agx",
    "exposureEv": 0.7
  }
}
```

---

# 23. Non-conforming examples

## 23.1. Unknown root property

```json
{
  "schema": "planaxis-design/1.0",
  "name": "Warm modern",
  "architecture": "architecture/existing.svg",
  "description": "Main living-room option"
}
```

Invalid because the root schema is closed and `description` is not a Version 1.0 property.

## 23.2. Empty `finishes`

```json
{
  "schema": "planaxis-design/1.0",
  "name": "Warm modern",
  "architecture": "architecture/existing.svg",
  "finishes": []
}
```

Invalid. Omit `finishes` when there are no assignments.

## 23.3. Duplicate finish target

```json
{
  "schema": "planaxis-design/1.0",
  "name": "Warm modern",
  "architecture": "architecture/existing.svg",
  "finishes": [
    {
      "target": "floor",
      "material": "assets/materials/oak/material.json"
    },
    {
      "target": "floor",
      "material": "assets/materials/tile/material.json"
    }
  ]
}
```

Invalid because one target may receive at most one persisted assignment.

## 23.4. Empty `presentation`

```json
{
  "schema": "planaxis-design/1.0",
  "name": "Warm modern",
  "architecture": "architecture/existing.svg",
  "presentation": {}
}
```

Invalid. Omit `presentation` when there are no presentation overrides.

## 23.5. Unknown presentation property

```json
{
  "schema": "planaxis-design/1.0",
  "name": "Warm modern",
  "architecture": "architecture/existing.svg",
  "presentation": {
    "toneMapping": "agx",
    "environmentIntensity": 2
  }
}
```

Invalid because `environmentIntensity` is not a Design 1.0 property.

## 23.6. Material outside `assets/materials/`

```json
{
  "schema": "planaxis-design/1.0",
  "name": "Warm modern",
  "architecture": "architecture/existing.svg",
  "finishes": [
    {
      "target": "floor",
      "material": "references/materials/oak-photo.jpg"
    }
  ]
}
```

Invalid because a Design 1.0 material reference must be under `assets/materials/`.

## 23.7. Invalid finish-target syntax

```json
{
  "schema": "planaxis-design/1.0",
  "name": "Warm modern",
  "architecture": "architecture/existing.svg",
  "finishes": [
    {
      "target": "some-wall",
      "material": "assets/materials/paint-white"
    }
  ]
}
```

Invalid because `some-wall` does not match any Version 1.0 finish-target lexical form.

## 23.8. Structurally valid but stale target

```json
{
  "schema": "planaxis-design/1.0",
  "name": "Previous concept",
  "architecture": "architecture/existing.svg",
  "finishes": [
    {
      "target": "wall:wall-old:side-positive",
      "material": "assets/materials/paint-white"
    }
  ]
}
```

Assuming `wall-old` satisfies the Apartment SVG `Id` lexical form, the target string is syntactically valid.

If the referenced architecture no longer produces that finish target, the descriptor may remain Design 1.0-format conformant but fails architecture resolution because the target is stale.

## 23.9. Applying a design to another architecture

Suppose the descriptor contains:

```json
"architecture": "architecture/existing.svg"
```

PlanAxis MUST NOT treat it as applicable to:

```text
architecture/variants/open-kitchen.svg
```

merely because every persisted finish target also happens to exist there.

Reuse requires an explicit design-editing operation that creates or updates a descriptor bound to the other architecture.

---

# 24. Conformance summary

A PlanAxis Design Format 1.0 implementation MUST at minimum enforce:

1. UTF-8 JSON containing exactly one object;
2. descriptor location at a lowercase `.json` path under `designs/`;
3. descriptor identity by project-relative path, with no internal ID field;
4. exact schema identifier `planaxis-design/1.0`;
5. required human-readable non-empty `name`;
6. required strict project-relative `architecture` binding under `architecture/` using lowercase `.svg`;
7. recursively closed object schemas with no unknown properties;
8. optional non-empty `finishes` array;
9. closed finish assignments containing exactly `target` and `material`;
10. normative Version 1.0 finish-target lexical forms;
11. no duplicate finish-target assignments;
12. material references using valid project-relative paths under `assets/materials/`;
13. no Design 1.0 requirement that material resources exist or have interpretable contents;
14. optional non-empty `presentation` object;
15. optional `toneMapping` limited to `agx`, `aces-filmic`, or `neutral`;
16. optional finite `exposureEv` without a schema-level UI range restriction;
17. strict distinction between Design Format conformance, project resolution, architecture resolution, and independent Material Format resolution;
18. complete Apartment SVG validation before finish-target resolution;
19. exact finish-target existence checks against the bound architecture;
20. strict prohibition on implicitly applying a design to another architecture;
21. no persistent active-design concept;
22. no silent repair or rewriting of stale references;
23. no renderer-specific object, enum, or GPU-resource serialization;
24. ordinary JSON numeric semantics rather than exact-decimal architectural geometry semantics;
25. explicit future versioning for furniture, lighting, environment assets, richer presentation state, or other new design concepts.

---

# 25. Final principle

A PlanAxis design descriptor expresses **design intent for one explicit architectural alternative**.

It does not become a second architectural model, a material definition, or a serialized renderer scene.

The persistent relationship is:

```text
Apartment SVG architecture
        +
PlanAxis Design scenario
        +
project-local assets
        ↓
resolved design state
        ↓
renderer adaptation
```

Architectural geometry remains authoritative in Apartment SVG.

Design Format 1.0 records only the supported design choices and references needed to layer a scenario on top of that architecture.
