{
  "name": "@planaxis/server",
  "version": "0.0.0",
  "private": true,
  "description": "PlanAxis HTTP server",
  "license": "Apache-2.0",
  "type": "module",
  "main": "./dist/index.js",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "start": "node dist/index.js",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "fastify": "5.12.1"
  },
  "devDependencies": {
    "vitest": "catalog:"
  }
}
