{
  "name": "@planaxis/cli",
  "version": "0.0.0",
  "private": true,
  "description": "PlanAxis Apartment SVG validation command-line application",
  "license": "Apache-2.0",
  "type": "module",
  "main": "./dist/index.js",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "start": "node dist/index.js",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@planaxis/parser": "workspace:*",
    "@planaxis/validator": "workspace:*"
  },
  "devDependencies": {
    "vitest": "catalog:"
  }
}
